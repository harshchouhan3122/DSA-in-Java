
public class Strings {

    public static void main(String args[]){
        String str = "Harsh Chouhan";


        // Length of the String
        System.out.println(str.length());

        // Search
        System.out.println(str.charAt(2));

        // Strings are immutable then hence its not possible -> Replace, Insert, Delete, Append
        // That'swhy we use StringBuilder class for the String manupulation purpose
    }
}
